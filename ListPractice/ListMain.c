#include <stdio.h>
#include "ArrayList.h"
/*
int main(void)
{
	List list;
	int data;
	ListInit(&list);

	for(int i=1;i<=9;i++) LInsert(&list,i);

	int hap = 0;
	if(LFirst(&list,&data))
	{
		hap+=data;
		while(LNext(&list,&data)) hap+=data;
	}
	printf("total sum : %d\n\n",hap);

	if(LFirst(&list,&data))
	{
		if(data%2==0||data%3==0) LRemove(&list);
		while(LNext(&list,&data))
		{
			if(data%2==0||data%3==0)
			{
				LRemove(&list);
			}
		}

	}

	if(LFirst(&list,&data))
	{
		printf("%d ",data);
		while(LNext(&list,&data)) printf("%d ",data);
	}

	return 0;
}
*/
