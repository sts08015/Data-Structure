/*#include <stdio.h>
#include "NameCard.h"
#include "ArrayList.h"
#include <stdlib.h>

int main()
{
    List list;
    NameCard* namecard;
    ListInit(&list);

    namecard = MakeNameCard("fisma","5484");
    LInsert(&list,namecard);
    printf("%d\n",LCount(&list));

    namecard = MakeNameCard("nayeon","8274");
    LInsert(&list,namecard);
    printf("%d\n",LCount(&list));

    namecard = MakeNameCard("sts08015","6020");
    LInsert(&list,namecard);

    printf("%d\n",LCount(&list));

    //int* test = 0;
    //printf("%d",*test);

    return 0;
}
*/
